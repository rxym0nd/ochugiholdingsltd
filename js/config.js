const CONFIG = {
  // Local development URL. When you deploy the backend to Render, 
  // change this to your live URL (e.g., 'https://ochugi-backend.onrender.com')
  API_URL: 'http://localhost:3000'
};
